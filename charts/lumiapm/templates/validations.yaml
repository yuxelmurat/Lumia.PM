{{- $publicExposureEnabled := or .Values.ingress.enabled .Values.gateway.enabled -}}
{{- if and (not .Values.lumiapm.env.existingSecret.enabled) (or (not .Values.lumiapm.env.authSecret) (lt (len (.Values.lumiapm.env.authSecret | toString)) 32)) -}}
{{- fail "lumiapm.env.authSecret must be explicitly set to at least 32 characters or provided via lumiapm.env.existingSecret" -}}
{{- end -}}
{{- if and $publicExposureEnabled .Values.postgresql.enabled (not .Values.postgresql.auth.existingSecret) (eq .Values.postgresql.auth.password "lumiapm_password") -}}
{{- fail "postgresql.auth.password must be changed or provided via postgresql.auth.existingSecret before enabling ingress or gateway" -}}
{{- end -}}
{{- if and .Values.autoscaling.enabled .Values.autoscaling.targetCPUUtilizationPercentage (not (dig "requests" "cpu" "" .Values.lumiapm.resources)) -}}
{{- fail "lumiapm.resources.requests.cpu must be set when autoscaling CPU utilization is enabled" -}}
{{- end -}}
