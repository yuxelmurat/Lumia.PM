name: Build & publish Helm chart to GHCR

on:
  pull_request:
    paths:
      - "charts/lumiapm/**"
      - ".github/workflows/helm-chart.yml"
  push:
    tags:
      - "lumiapm-*"
    paths:
      - "charts/lumiapm/**"
      - ".github/workflows/helm-chart.yml"
  workflow_dispatch:
    inputs:
      version:
        description: "Version to tag the images (e.g., 1.0.0)"
        required: true
        type: string

env:
  CHART_PATH: charts/lumiapm
  REGISTRY: ghcr.io

jobs:
  validate:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: default
            args: >-
              --set lumiapm.env.authSecret=secure-auth-secret-at-least-32-characters
          - name: ingress
            args: >-
              --set ingress.enabled=true
              --set lumiapm.env.authSecret=secure-auth-secret-at-least-32-characters
              --set postgresql.auth.password=secure-db-password
          - name: gateway
            args: >-
              --set gateway.enabled=true
              --set gateway.parentRefs[0].name=gateway
              --set lumiapm.env.authSecret=secure-auth-secret-at-least-32-characters
              --set postgresql.auth.password=secure-db-password
          - name: autoscaling
            args: >-
              --set autoscaling.enabled=true
              --set lumiapm.resources.requests.cpu=100m
              --set lumiapm.env.authSecret=secure-auth-secret-at-least-32-characters
          - name: external-database
            args: >-
              --set postgresql.enabled=false
              --set lumiapm.env.database.external.enabled=true
              --set lumiapm.env.database.external.host=db.example.com
              --set lumiapm.env.database.external.password=secure-db-password
              --set lumiapm.env.authSecret=secure-auth-secret-at-least-32-characters
          - name: bundled-postgresql-secret
            args: >-
              --set postgresql.auth.existingSecret=lumiapm-secrets
              --set lumiapm.env.authSecret=secure-auth-secret-at-least-32-characters

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
        with:
          persist-credentials: false

      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5

      - name: Lint chart
        run: helm lint "$CHART_PATH"

      - name: Render chart (${{ matrix.name }})
        run: helm template lumiapm "$CHART_PATH" ${{ matrix.args }}

  publish:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-24.04
    needs: validate
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
        with:
          persist-credentials: false

      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5
    
      - name: Extract Version from Tag
        env:
          INPUT_VERSION: ${{ github.event.inputs.version }}
        run: |
          if [[ $GITHUB_REF == refs/tags/lumiapm-* ]]; then
            echo "CHART_VERSION=${GITHUB_REF#refs/tags/lumiapm-}" >> $GITHUB_ENV
          else
            echo "CHART_VERSION=$INPUT_VERSION" >> $GITHUB_ENV
          fi
    
      - name: Package chart
        run: |
          mkdir -p dist
          helm package "$CHART_PATH" --version "$CHART_VERSION" --app-version "$CHART_VERSION" --destination dist
    
      - name: Log in to GHCR
        env:
          GH_ACTOR: ${{ github.actor }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: echo "$GH_TOKEN" | helm registry login "$REGISTRY" --username "$GH_ACTOR" --password-stdin
    
      - name: Publish chart
        run: |
          OWNER="${GITHUB_REPOSITORY_OWNER,,}"
          CHART_REF="oci://$REGISTRY/$OWNER/charts/lumiapm"

          if helm show chart "$CHART_REF" --version "$CHART_VERSION" >/dev/null 2>&1; then
            echo "Chart $CHART_REF:$CHART_VERSION already exists; skipping publish."
            exit 0
          fi

          helm push dist/lumiapm-"$CHART_VERSION".tgz "oci://$REGISTRY/$OWNER/charts"
